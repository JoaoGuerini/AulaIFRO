/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true,
      srcDir: 'src/aula-1-react',
    },
  }
  
  module.exports = nextConfig